# C# 프로그래밍 추가 종합 평가 문제지 (총 50문항)

**출제 범위:** C# 개요, 빌드 과정, 배열(1/2차원, 가변), 파일 I/O 및 직렬화, 예외 처리, 속성과 인덱서, 인터페이스  
**문항 구성:** 객관식 25문항, 주관식 20문항, 손코딩 5문항

---

## Part 1. 객관식 (1~25번)

### 1. [객관식] 마이크로소프트에서 2000년에 발표한 C# 언어가 구동되는 핵심 실행 환경의 이름은?

① Java Virtual Machine  
② .NET Framework  
③ Node.js  
④ Eclipse  

### 2. [객관식] C# 프로그램의 빌드 과정 중, 사람이 이해하는 소스 코드(*.cs)를 컴퓨터가 이해하는 기계어로 변환하여 오브젝트 파일을 생성하는 과정은?

① 코딩 (Coding)  
② 링크 (Link)  
③ 컴파일 (Compile)  
④ 실행 (Execute)  

### 3. [객관식] Visual Studio에서 C# 프로그램을 작성할 때 생성되는 소스 파일의 기본 확장자는?

① .c  
② .cpp  
③ .cs  
④ .exe  

### 4. [객관식] 1차원 배열을 선언하고 생성하는 문법으로 올바른 것은?

① int numbers[] = new int[5];  
② int[] numbers = new int(5);  
③ int[] numbers = new int[5];  
④ Array numbers = new int[5];  

### 5. [객관식] 배열의 크기(요소의 총 개수)를 알아내기 위해 사용하는 속성은?

① Size  
② Length  
③ Count  
④ Capacity  

### 6. [객관식] 행(Row)과 열(Column)로 구성된 2차원 배열의 선언으로 올바른 것은?

① int[][] arr = new int[2][3];  
② int[,] arr = new int[2, 3];  
③ int[2,3] arr = new int[];  
④ int arr[,] = new int[2, 3];  

### 7. [객관식] 각 행마다 열의 길이가 다를 수 있는 '배열의 배열' 구조를 뜻하는 용어는?

① 1차원 배열  
② 2차원 배열  
③ 3차원 배열  
④ 가변 배열 (Jagged Array)  

### 8. [객관식] 파일 처리 시, 프로그램이 종료된 후에도 데이터를 영구적으로 유지하기 위해 사용하는 비휘발성 저장소는?

① RAM  
② CPU 레지스터  
③ 파일(File)  
④ 캐시(Cache)  

### 9. [객관식] C#에서 파일 경로에 파일이 존재하는지 확인하기 위해 사용하는 정적 메서드는?

① File.Open()  
② File.Exists()  
③ File.Check()  
④ File.Find()  

### 10. [객관식] 바이트 단위로 대용량 파일을 읽고 쓰며, 좀 더 세밀한 제어가 필요할 때 사용하는 클래스는?

① StreamWriter  
② FileStream  
③ StreamReader  
④ TextWriter  

### 11. [객관식] FileStream의 FileMode 중, 기존 파일이 있으면 열고 내용의 맨 뒤에 데이터를 이어쓰기 위한 모드는?

① Create  
② Open  
③ Append  
④ Truncate  

### 12. [객관식] 버퍼(Buffer)에 쌓여 있는 데이터를 실제 파일로 강제로 밀어넣어 저장되게 하는 메서드는?

① Close()  
② Write()  
③ Flush()  
④ Seek()  

### 13. [객관식] 텍스트 파일에서 문자열 데이터를 읽어올 때 사용하는 클래스는?

① BinaryReader  
② StreamReader  
③ BinaryWriter  
④ FileStream  

### 14. [객관식] JSON 형식에 대한 설명으로 틀린 것은?

① JavaScript Object Notation의 약자이다.  
② 객체는 대괄호 []로 묶어서 표현한다.  
③ 키와 값(Key: Value) 구조로 이루어져 있다.  
④ 사람이 읽기 쉽고 언어 간 호환성이 좋다.  

### 15. [객관식] 객체(Object)를 파일로 저장하거나 네트워크로 전송할 수 있는 형태(예: JSON)로 변환하는 과정을 무엇이라 하는가?

① 직렬화 (Serialization)  
② 역직렬화 (Deserialization)  
③ 캡슐화 (Encapsulation)  
④ 다형성 (Polymorphism)  

### 16. [객관식] 프로그램 실행 중 발생하는 예기치 못한 오류를 의미하며, 이를 처리하여 프로그램 강제 종료를 막는 기법은?

① 디버깅 (Debugging)  
② 예외 처리 (Exception Handling)  
③ 컴파일링 (Compiling)  
④ 가비지 컬렉션 (Garbage Collection)  

### 17. [객관식] try-catch 구문에서 예외가 발생할 가능성이 있는 코드를 작성하는 블록은?

① try  
② catch  
③ finally  
④ throw  

### 18. [객관식] 여러 개의 catch 블록을 사용할 때 올바른 작성 원칙은?

① 부모 클래스인 Exception을 가장 먼저 작성해야 한다.  
② 예외 종류와 무관하게 순서는 상관없다.  
③ 구체적인 예외부터 먼저 작성하고, 일반적인 예외(Exception)를 나중에 작성한다.  
④ 하나의 try문에는 반드시 하나의 catch문만 작성할 수 있다.  

### 19. [객관식] 예외 발생 여부와 상관없이 파일 닫기, DB 연결 해제 등의 마무리 작업을 위해 반드시 실행되어야 하는 블록은?

① try  
② catch  
③ finally  
④ using  

### 20. [객관식] 비즈니스 규칙 위반이나 잘못된 입력 시 프로그래머가 강제로 예외를 발생시킬 때 사용하는 키워드는?

① catch  
② throw  
③ error  
④ break  

### 21. [객관식] 클래스의 필드(데이터)를 외부에서 직접 접근하지 못하게 보호하고, 간접적으로 제어하며 읽기/쓰기를 지원하는 멤버는?

① 이벤트 (Event)  
② 대리자 (Delegate)  
③ 속성 (Property)  
④ 네임스페이스 (Namespace)  

### 22. [객관식] 외부에서는 읽기만 가능하고, 클래스 내부에서만 값을 수정할 수 있도록(쓰기) 설정하는 속성의 올바른 문법은?

① public int Age { get; }  
② public int Age { private get; set; }  
③ public int Age { get; private set; }  
④ public int Age { get; set; }  

### 23. [객관식] 객체를 배열처럼 대괄호([])를 사용하여 접근할 수 있게 해주는 기능은?

① 인덱서 (Indexer)  
② 프로퍼티 (Property)  
③ 인터페이스 (Interface)  
④ 생성자 (Constructor)  

### 24. [객관식] 인터페이스(Interface)의 특징으로 옳은 것은?

① 내부에 메서드의 구체적인 구현부({ ... })를 포함할 수 있다.  
② new 키워드를 통해 직접 객체를 생성할 수 있다.  
③ 클래스에서 다중 상속(구현)이 가능하다.  
④ 접근 제한자로 private를 사용하여 멤버를 숨길 수 있다.  

### 25. [객관식] 다음 중 다형성을 구현하고 객체 간 결합도를 낮추기 위해 "기능의 규약(약속)"을 정의하는 데 가장 적합한 것은?

① abstract class  
② struct  
③ interface  
④ enum  

---

## Part 2. 주관식 (26~45번)

### 26. [주관식]
> MS가 제공하는 C# 개발 환경으로, 코딩, 컴파일, 디버깅을 한 곳에서 할 수 있는 통합 개발 환경의 이름은 무엇인가?

### 27. [주관식]
> 소스 파일을 기계어로 변환하는 과정을 '컴파일'이라고 한다면, 여러 오브젝트 파일과 관련 라이브러리를 하나로 결합하여 최종 실행 파일(.exe)을 생성하는 과정을 무엇이라고 하는가?

### 28. [주관식]
> 1차원 배열 int[] arr = { 10, 20, 30 }; 이 있을 때, 20이라는 값을 출력하기 위해 접근해야 하는 인덱스 번호는 몇 번인가?

### 29. [주관식]
> 2차원 배열 int[,] map = new int[4, 5];가 선언되었다. 이 배열이 저장할 수 있는 요소(데이터)의 총 개수는 몇 개인가?

### 30. [주관식]
> 각 행(Row)마다 열(Column)의 개수를 다르게 지정할 수 있어 int[][] 형태로 선언하는 배열을 무엇이라고 하는가?

### 31. [주관식]
> FileStream을 사용하여 파일의 특정 위치로 읽기/쓰기 포인터를 이동시킬 때 사용하는 메서드 이름은 무엇인가?

### 32. [주관식]
> 텍스트 파일에서 한 줄씩 데이터를 읽어올 때 StreamReader 클래스에서 주로 사용하는 메서드는 무엇인가?

### 33. [주관식]
> 메모리 상의 객체를 문자열이나 바이트 형태로 변환하는 것을 '직렬화'라고 한다. 반대로 파일에 저장된 JSON 데이터를 다시 C# 객체(Object)로 변환하는 과정을 무엇이라고 하는가?

### 34. [주관식]
> 예외 처리 구문에서 예외가 발생했을 때 이를 잡아내어 대체 로직을 수행하도록 하는 블록(키워드)은 무엇인가?

### 35. [주관식]
> 모든 C# 예외 클래스의 최상위 부모 클래스 이름은 무엇인가?

### 36. [주관식]
> 속성(Property) 정의 시, 외부에서 들어온 값을 필드에 저장하기 위해 set 블록 내에서 사용하는 암묵적인 매개변수 키워드는 무엇인가?

### 37. [주관식]
> 클래스 내부의 배열이나 리스트를 객체 자체에서 obj[0] 처럼 접근할 수 있도록 정의할 때, 이름 대신 사용하는 키워드는 무엇인가? (Hint: public int ____ [int index])

### 38. [주관식]
> 인터페이스의 이름을 명명할 때, 일반적인 클래스와 구분하기 위해 관례상 이름 맨 앞에 붙이는 대문자 알파벳은 무엇인가?

### 39. [주관식]
> 인터페이스는 메서드의 선언만 가질 뿐, 무엇을 가질 수 없는가? (Hint: 로직이 들어가는 괄호 { } 부분)

### 40. [주관식]
> 인터페이스를 클래스에 상속(구현)시킬 때 사용하는 기호는 무엇인가? (예: class Dog ___ IAnimal)

### 41. [주관식]
> FileStream 생성 시 기존에 있는 파일의 내용을 유지한 채 뒷부분에 데이터를 덧붙이고자 할 때 FileMode는 무엇으로 설정해야 하는가?

### 42. [주관식]
> 1~45 사이의 무작위 난수를 생성하기 위해 사용하는 C#의 내장 클래스 이름은 무엇인가?

### 43. [주관식]
> 속성(Property)에서 값을 가져갈 때(읽을 때) 실행되는 블록의 이름은 무엇인가?

### 44. [주관식]
> 컴파일 오류와 달리 프로그램이 '실행 중'에 0으로 나누기, 파일 없음 등으로 인해 발생하는 오류를 통틀어 무엇이라고 하는가?

### 45. [주관식]
> StreamWriter를 통해 데이터를 파일에 기록할 때, 데이터를 임시로 모아두는 메모리 공간을 무엇이라고 하는가?

---

## Part 3. 손코딩 (46~50번)

### 46. [손코딩]
> 크기가 5인 정수형 1차원 배열 scores를 선언 및 생성하고, 반복문(for)을 사용하여 0번부터 4번 인덱스에 각각 10, 20, 30, 40, 50을 저장하는 코드를 작성하시오.

### 47. [손코딩]
> IAttack 이라는 인터페이스를 선언하고, 그 안에 반환형이 없는 Attack() 메서드를 선언(정의)하시오.

### 48. [손코딩]
> int result = 10 / 0; 연산을 수행하려 합니다. 이 코드를 try 블록에 넣고, 0으로 나누었을 때 발생하는 예외(DivideByZeroException)를 처리하는 catch 블록을 작성하여 콘솔에 "오류 발생"이라고 출력하도록 예외처리 구문을 완성하시오.

### 49. [손코딩]
> Player 클래스 내부에 체력을 나타내는 hp라는 private 정수형 필드가 있습니다. 외부에서 값을 읽고 쓸 수 있는 HP라는 속성(Property)을 만들되, set 블록에서 들어오는 값(value)이 0보다 작으면 hp를 0으로 고정시키고, 그렇지 않으면 들어온 값을 hp에 저장하는 코드를 작성하시오.

### 50. [손코딩]
> 문자열 데이터를 텍스트 파일("test.txt")에 저장하려고 합니다. StreamWriter 클래스를 사용하여 파일을 생성(또는 덮어쓰기)하고, "Hello C#"이라는 문자열을 한 줄 기록한 뒤, 파일을 안전하게 닫는(Close) 코드를 작성하시오.

---

## [정답 및 해설]

### Part 1. 객관식
1. ② (.NET Framework 위에서 구동됩니다.)  
2. ③ (기계어로 번역하여 오브젝트 파일을 만드는 것은 컴파일입니다.)  
3. ③ (C# 소스 파일 확장자는 .cs 입니다.)  
4. ③ (자료형 뒤에 []를 붙이고 new 연산자로 크기를 지정합니다.)  
5. ② (배열명.Length를 사용하여 크기를 구합니다.)  
6. ② (2차원 배열은 대괄호 안에 콤마를 사용하여 [,]로 선언합니다.)  
7. ④ (배열의 배열 구조를 가지며 각 행의 길이가 유동적인 것은 가변 배열입니다.)  
8. ③ (메모리는 휘발성, 파일은 비휘발성입니다.)  
9. ② (File.Exists(경로)를 사용해 파일 유무를 판단합니다.)  
10. ② (바이트 단위의 입출력 및 세밀한 제어는 FileStream 클래스입니다.)  
11. ③ (Append 모드는 이어쓰기를 의미합니다.)  
12. ③ (Flush()는 버퍼의 내용을 파일로 강제 전송합니다.)  
13. ② (텍스트 파일 읽기에는 주로 StreamReader가 사용됩니다.)  
14. ② (JSON에서 객체는 중괄호 {}, 배열이 대괄호 []입니다.)  
15. ① (객체를 전송/저장 포맷으로 바꾸는 것을 직렬화라 합니다.)  
16. ② (실행 중 오류를 제어하는 기법은 예외 처리입니다.)  
17. ① (문제가 발생할 수 있는 코드는 try 안에 둡니다.)  
18. ③ (하위/구체적 예외를 먼저 잡고, 최상위인 Exception을 마지막에 잡아야 합니다.)  
19. ③ (finally 블록은 예외 여부와 상관없이 무조건 실행됩니다.)  
20. ② (throw 키워드로 강제 예외 발생이 가능합니다.)  
21. ③ (메서드처럼 제어하면서 변수처럼 쓰이는 속성입니다.)  
22. ③ (get은 public으로 열고, set을 private으로 닫습니다.)  
23. ① (클래스 객체를 배열 인덱스처럼 다루게 해주는 것이 인덱서입니다.)  
24. ③ (인터페이스는 다중 상속이 가능하며 로직, 객체 생성이 불가합니다.)  
25. ③ (구현체 없이 기능의 명세만 선언하는 인터페이스가 적합합니다.)  

### Part 2. 주관식
26. Visual Studio (비주얼 스튜디오)  
27. 링크 (Link)  
28. 1 (arr[1]에 20이 저장됨)  
29. 20 (4 x 5 = 20개)  
30. 가변 배열 (Jagged Array)  
31. Seek (또는 Seek())  
32. ReadLine (또는 ReadLine())  
33. 역직렬화 (Deserialization)  
34. catch  
35. Exception  
36. value  
37. this  
38. I (대문자 아이)  
39. 구현부 (또는 내부 로직, 몸통, Body)  
40. : (콜론)  
41. Append (또는 FileMode.Append)  
42. Random  
43. get (또는 get 블록)  
44. 예외 (Exception)  
45. 버퍼 (Buffer)  

### Part 3. 손코딩

**46번 정답 예시:**

```csharp
int[] scores = new int[5];
for (int i = 0; i < scores.Length; i++)
{
    scores[i] = (i + 1) * 10;
}
```

**47번 정답 예시:**

```cs
interface IAttack
{
    void Attack();
}
```

**48번 정답 예시:**

```csharp
try
{
    int result = 10 / 0;
}
catch (DivideByZeroException)
{
    Console.WriteLine("오류 발생");
}
```

**49번 정답 예시:**

```cs
private int hp;
public int HP
{
    get { return hp; }
    set 
    {
        if (value < 0)
        {
            hp = 0;
        }
        else
        {
            hp = value;
        }
    }
}
```

**50번 정답 예시:**

```csharp
StreamWriter sw = new StreamWriter("test.txt");
sw.WriteLine("Hello C#");
sw.Close();
```    